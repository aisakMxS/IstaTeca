<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
    </data>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/listarbilbiotecarioss"
        tools:context=".ui.listado_bibliotecario.listado.bibliotecarioFragment">

        <ListView
            android:id="@+id/lista_bibliotecarios"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:listitem="@layout/lista_item_bibliotecario" />
        <TextView
            android:id="@+id/empty"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:textColor="@color/black"
            android:textSize="24dp"
            android:text="NO SE ENCUENTRA DATOS"
            android:visibility="gone"
            />
    </FrameLayout>
</layout>